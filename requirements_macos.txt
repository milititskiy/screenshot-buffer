# requirements_macos.txt — macOS Sequoia (15.x) dependencies
# Install with: pip install -r requirements_macos.txt

# ── Core UI & event loop ──────────────────────────────────────────────
PySide6>=6.7.0

# ── Screen capture ────────────────────────────────────────────────────
mss>=9.0.1
Pillow>=10.3.0

# ── Global hotkeys (uses CGEventTap on macOS) ─────────────────────────
pynput>=1.7.7

# ── macOS native clipboard (NSPasteboard) + Dock hiding ───────────────
# pyobjc-framework-Cocoa pulls in pyobjc-core automatically.
# Pin to >=10.3 for Sequoia (15.x) compatibility.
pyobjc-core>=10.3.1
pyobjc-framework-Cocoa>=10.3.1

# ── Packaging (not needed at runtime — install separately) ────────────
# pyinstaller>=6.6.0
